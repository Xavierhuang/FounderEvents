import SwiftUI

// MARK: - Founder-Friendly Color Scheme
extension Color {
    // Primary brand colors - Professional blue scheme
    static let founderPrimary = Color(red: 0.10, green: 0.30, blue: 0.60) // Deep blue
    static let founderAccent = Color(red: 0.20, green: 0.50, blue: 0.85) // Vibrant blue
    static let founderSecondary = Color(red: 0.40, green: 0.60, blue: 0.85) // Soft blue
    
    // Gradient colors for backgrounds
    static let founderGradientStart = Color(red: 0.15, green: 0.40, blue: 0.70) // Deep blue
    static let founderGradientMiddle = Color(red: 0.25, green: 0.50, blue: 0.80) // Medium blue
    static let founderGradientEnd = Color(red: 0.35, green: 0.60, blue: 0.90) // Light blue
    
    // UI element colors
    static let founderCardBackground = Color(red: 0.98, green: 0.98, blue: 0.99) // Off-white
    static let founderBorder = Color(red: 0.85, green: 0.85, blue: 0.90) // Light gray
    
    // Status colors (professional versions)
    static let founderSuccess = Color(red: 0.20, green: 0.65, blue: 0.40) // Professional green
    static let founderWarning = Color(red: 0.95, green: 0.65, blue: 0.15) // Warm amber
    static let founderError = Color(red: 0.85, green: 0.25, blue: 0.25) // Muted red
    
    // Text colors
    static let founderTextPrimary = Color(red: 0.15, green: 0.15, blue: 0.20) // Almost black
    static let founderTextSecondary = Color(red: 0.45, green: 0.45, blue: 0.50) // Medium gray
}

